name: Scanner Bot
on:
  schedule:
    - cron: '0 16 * * *'   # 8:00 AM PT
    - cron: '0 0 * * *'    # 4:00 PM PT
    - cron: '0 8 * * *'    # midnight PT
  workflow_dispatch:
    inputs:
      pattern:
        description: 'Vulnerability pattern'
        type: choice
        options:
          - rotate
          - shell-injection
          - shell-injection-body
          - shell-injection-headref
          - shell-injection-actor
          - dangerous-triggers
      dry-run:
        description: 'Dry run'
        type: boolean
        default: true

permissions:
  contents: read

jobs:
  scan:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6.0.3
        with:
          persist-credentials: false
      - uses: ruby/setup-ruby@afeafc3d1ab54a631816aba4c914a0081c12ff2f # v1.310.0
        with:
          ruby-version: '3.3'
      - name: Run bot
        env:
          GITHUB_TOKEN: ${{ secrets.BOT_GITHUB_TOKEN }}
          PATTERN: ${{ inputs.pattern || 'rotate' }}
          DRY_RUN: ${{ inputs.dry-run }}
        run: |
          args="--queue --limit 100 --pattern $PATTERN"
          if [ "$DRY_RUN" = "true" ]; then args="$args --dry-run"; fi
          ruby -Ilib -Ibot bot/scanner_bot.rb $args
